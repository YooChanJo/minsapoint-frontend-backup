import { Text, View } from "react-native";

/* Add splash screen == the screen that is presented when loading over here */
export default function SplashScreen() {
  return (
    <View>
      <Text>Splash Screen</Text>
    </View>
  );
}
